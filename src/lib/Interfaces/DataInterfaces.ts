export interface Iphotos {
    albumId: number
    id: number
    title: string
    url: string
    thumbnailUrl: string
}

export interface Icart {
    listingId: number
    title: string
    price: number
    image: string
}

export interface ItestListing {
  albumId: number
  id: number
  title: string
  url: string
  thumbnailUrl: string
}